evaluations.c reads the model chosen in ../g.c,
then accept the required parameters by command line,
printing the results on the screen.
Redirecting them on a file, we use gnuplot
to generate a prediction's picture.
